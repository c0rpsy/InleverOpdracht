<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Crow.MVVM.ViewModels"
             x:Class="Crow.MVVM.Views.AssignmentPage"
             Title="{Binding ThemeName}">
    <ContentPage.BindingContext>
        <vm:AssignmentViewModel />
    </ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding Assignments}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Label Text="{Binding Name}" FontSize="18" Grid.Row="0" Grid.Column="0" />
                    <Image Source="{Binding PhotoPath}" HeightRequest="100" WidthRequest="100" Grid.Row="1" Grid.Column="0" />

                    <Button Text="Take Photo"
                            Command="{Binding BindingContext.TakePhotoCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                            CommandParameter="{Binding Id}"
                            Grid.Row="1"
                            Grid.Column="1" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>